import React, { useState, useEffect } from "react";
import '../App.css';

const Work = (props) => {
console.log(props)

    return (
        <div>
            <div>
            {/*     {props.eduList.map(item => (
                    <div key={item.id}>
                        <p >{item.value.school}</p>
                        <p >{item.value.major}</p>
                    </div>

                ))} */}

            </div>
        </div>
    );


}

export default Work;